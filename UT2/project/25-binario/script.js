/**
 * Ejercicio 25 — Binario
 * (Sentencias: bucles)
 * --------------------------------------------------------------------
 *
 * Para pasar un número decimal a binario, se realizan divisiones sucesivas
 * entre 2, anotando el resto (0 o 1) en cada paso. Cuando el cociente llega
 * a 0, los restos leídos en orden inverso forman el número en binario.
 *
 * Haz un script que pida un número n y escriba por consola su representación
 * en binario. Si el valor introducido es incorrecto (solo se admiten enteros
 * positivos), no debe mostrar nada por consola.
 */

let n = prompt("Introduce un número:");

