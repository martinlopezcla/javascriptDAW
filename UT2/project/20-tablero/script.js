/**
 * Ejercicio 20 — Tablero
 * (Sentencias: bucles)
 * --------------------------------------------------------------------
 *
 * Crea un script que pida dos números (columnas y filas) y muestre por
 * consola un "tablero de ajedrez" hecho con almohadillas. Si el usuario
 * introduce un valor incorrecto (solo se admiten enteros positivos), no debe
 * mostrar nada por consola.
 *
 * Ejemplo: consulta index.html en esta misma carpeta para ver una
 * imagen de referencia (tablero de 7 columnas x 4 filas).
 */

let columnas = prompt("Número de columnas:");
let filas = prompt("Número de filas:");

