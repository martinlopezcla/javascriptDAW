/**
 * Ejercicio 18 — Pedir número mayor que 100
 * (Sentencias: bucles)
 * --------------------------------------------------------------------
 *
 * Escribe un bucle que solicite un número mayor que 100. Si el usuario
 * introduce otro número, pídele que lo introduzca de nuevo.
 *
 * El bucle debe pedir un número hasta que el usuario introduzca uno mayor
 * que 100, o bien cancele la entrada o introduzca una línea vacía.
 *
 * Puedes asumir que el usuario solo introducirá números: no hace falta
 * gestionar entradas no numéricas en este ejercicio.
 */

let numero;

do {
  numero = prompt("Introduce un número mayor que 100:");
} while ( /* condición */ );
